---
refresh_rate: 5 seconds

appenders:
  stdout:
    kind: console

    encoder:
      kind: pattern
      pattern: "{level:>5} {file}:{line} {message}{n}"

root:
  level: warn
  appenders:
    - stdout

loggers:
  tiny_auth:
    level: trace
  actix_session:
    level: trace
