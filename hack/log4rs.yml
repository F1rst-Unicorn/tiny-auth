---
refresh_rate: 5 seconds

appenders:
  stdout:
    kind: console

    encoder:
      kind: pattern
      pattern: "{level:>5} {file}:{line} {message}{n}"

root:
  level: trace
  appenders:
    - stdout
